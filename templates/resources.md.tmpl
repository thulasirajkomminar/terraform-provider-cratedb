---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

{{/* Example and import files live in directories named without the provider prefix, e.g. examples/resources/cluster for cratedb_cluster. */}}
{{- $shortName := index (split .Name (printf "%s_" .ProviderShortName)) 1 }}

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

{{ tffile (printf "examples/resources/%s/resource.tf" $shortName) }}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the following syntax:

{{ codefile "shell" (printf "examples/resources/%s/import.sh" $shortName) }}
